# 第 3 项中性原始证据包

## 一、本项身份和问题

- 固定编号：`C10-CAP-02`
- 名称：止损价格数量演变与替换候选
- 性质：中性证据汇编，不是答案或执行授权

本项只判断：现有材料能否还原已捕获止损委托的时间、价格、数量和终态变化；相邻止损能否被认定为同一次“修改／替换”；严格证明与满足用户理解交易过程之间有什么差别。

## 二、用户直接原话

### 2.1 用户要求看到止盈止损完整过程

W01，`Prompt 2026/7/19 02:50:50`，第 69,442—69,467 行：

> 希望还原：
>
> - 首次设置止盈的时间、数量、触发价格；
> - 首次设置止损的时间、数量、触发价格；
> - 后续止盈止损是否取消；
> - 是否修改触发价格；
> - 是否由新条件委托替换；
> - 是否发生数量调整；
> - 是否部分覆盖仓位；
> - 是否全部覆盖仓位；
> - 是否触发；
> - 是否成交；
> - 是否过期；
> - 是否因仓位已经通过其他订单平仓而自动失效。
>
> 需要重点区分：
>
> 1. 条件委托被用户主动取消；
> 2. 条件委托被新的止盈止损替换；
> 3. 仓位已经结束后，原条件委托自动过期；
> 4. 条件委托真正触发并生成基础单；
> 5. 条件委托触发后，基础单完全成交；
> 6. 条件委托触发但未完全成交；
> 7. 普通限价平仓导致原止盈止损失效。

### 2.2 用户要求证据充分才能认定替换关系

同一条用户消息，第 69,599—69,621 行：

> 旧条件委托取消
> → 短时间内出现同一合约、同一持仓方向、相近数量的新条件委托
> → 新触发价格不同
> → 旧委托可能被新委托替换
>
> 但这种关系只能在证据充分时认定。
>
> 请设计置信度等级，例如：A级客观确认、B级高置信度推断、C级一般可能、D级无法判断。
>
> 每条关联关系需要说明使用了哪些字段、满足了哪些规则、是否存在其他合理解释、最终置信度和是否需要人工确认。

### 2.3 用户描述持仓过程中反复修改或取消止损

W01，`Prompt 2026/7/20 15:20:24`，第 90,522 行：

> ……包括设置市价止损后又不断的修改或者取消，最终止损或者限价平仓后才计入减仓或者平仓，这个过程也看不到……

这句话是用户对交易过程和显示需求的说明，不是每张止损委托的逐项确认。

## 三、当时 Chat 提出的历史办法和限制

以下是历史人工智能的方案，不是交易所事实。

W01，`Response 2026/7/20 15:38:18`，第 91,154—91,209 行：

> 条件委托也必须放入当前仓位的持仓窗口。
>
> 例如空头仓位期间：设置止损65,000，取消；设置止损64,500，取消；设置止损64,000；最后仓位通过限价平空归零，最后一张止损显示已过期。
>
> 最终显示止损设置、止损取消、疑似止损调整、是否实际触发、最终实际出场以及最后一张止损的终态。
>
> 已取消，之后没有新条件单，可以确认该止盈或止损条件委托被取消；无法确认用户为什么取消、是否临时不想止损、是否系统取消。
>
> 已取消，紧接着出现新条件单，如果满足同合约、同方向、同类型、触发价改变、数量相近、仓位未结束，可以识别为“疑似修改或替换”。
>
> 已完成必须继续检查明确触发时间、生成订单号、对应基础单、真实分笔成交以及是否使仓位减少或归零；只有完整匹配后，才算真正触发成交。

## 四、历史执行回报

W01，用户转贴的`Prompt 2026/7/20 18:09:12`，第 94,488—94,516 行中的相关数据：

> 读取条件委托总数543；与10笔相关80；A/B/C/D归属为3、70、7、0；止损设置57；止损取消50；止损过期4；疑似止损调整43；止损实际触发、成交和最终平仓均为3；需要人工确认13；没有强制升级C/D归属。

这是十笔样本的历史执行回报。它报告存在止损时间线和疑似调整计数，但“疑似”不等于用户逐张确认了修改关系。

## 五、两份十笔母版本体的只读结构

两份母版的隐藏工作表`技术_条件委托演变`具有相同表头，直接包含：

- 创建时间、结束时间、与开仓和平仓的时间关系；
- 条件类型原文与中文类型、类型是否推断；
- 触发价格、委托价格、委托数量、最终状态；
- 是否触发、是否成交、是否影响仓位、是否使仓位归零；
- 与上一张条件委托关系、旧触发价格、新触发价格、时间间隔、数量差；
- 关系等级、归属等级、中文事实说明、人工确认；
- 算法ID、来源、原因状态、证据等级。

该结构说明旧工程保存过逐次值和候选关系。结构与派生字段存在，不等于每一条替换原因都具有权威直接来源。

两份工作簿身份：

- `F2.5.4_M4-F2仓位链连续性修正版.xlsx`：275,373字节；SHA-256 `5d6a0b3236bbda2fae443d0a51a4ee9a33537538119a17b067d37a1038332d4a`
- `K08_PC1_RW003_10笔用户产品后置纠偏候选.xlsx`：299,519字节；SHA-256 `e095257905888b54ec0dbeda71055c0bf32489c5c4096b5bac8e0daba236c044`

## 六、已经冻结或验收的历史记录

这些是需要重新评估的历史记录，不是本轮答案。

阶段一用户验收清单，第 655—660 行：

> **C10-CAP-02｜止损价格数量演变与替换候选**：止损每次已捕获设置的价格、数量、时间与结束先后可比较；不能把它称为已确认的改单/替换链。部分或全部权威事实仍缺；保留明确缺口。
>
> 已存328相邻候选保留为REFERENCE，不将启发式候选升级为已证改单/止损变化。

阶段三最终裁定，第 99 行：

> `C10-CAP-02 | 止损价格数量演变与替换候选 | ……能力仍需要，但缺失部分继续未知 | CLEAN-CANDIDATE-96657b2714029a86、CLEAN-CANDIDATE-3c415708c072ac29`

后来查证成果，第 204 行：

> `C10-CAP-02 | 冻结记录保留未决 | S3INPUT-06、S3INPUT-21 | 条件演变中的原状态、疑似相邻替换及覆盖分层保留；用户语义与后续限定分开；无正式关联不等于没设置，弱归属不能升级，原因与全覆盖仍未独立证明。`

## 七、独立判断要求

请用约一千五百至两千五百个中文字符覆盖角色规则的九项内容，并分别回答：

1. 已捕获止损的价格、数量、时间和终态演变是否可用；
2. “疑似连续调整／替换”和“已确认修改”之间还差什么证明；
3. 对一百三十六笔交易链而言，是否可以保留演变时间线和候选关系，应该用什么证据身份；
4. 具体影响哪些止损字段、中文说明和关系等级。

完成第 3 项后停止，不处理覆盖比例、自动失效原因或止盈项目。
