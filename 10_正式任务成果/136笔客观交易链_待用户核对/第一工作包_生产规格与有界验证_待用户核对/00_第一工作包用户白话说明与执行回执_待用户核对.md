# 第一工作包｜用户白话说明与执行回执（待用户核对）

## 一、这次到底做了什么

这次完成的是“正式建立136笔客观交易链以前的生产准备和小范围验证候选”。

它做了五件事：

1. 把阶段三留下的44项输入候选，逐项转成可供用户以后决定“采用、缩小或拒绝”的准确清单。
2. 对这44项中的74次对象引用和72条不重复的实际路径逐一核对身份、选择范围和装载结果。
3. 把建链需要的组成部分、能力、输入、未决缺口、阶段三以后新发现和三项以后重新计算要求，放进同一张对应关系中。
4. 形成可重复运行的候选程序、配置、数据规则和十五项最小程序测试；候选程序既能重建74个获准输入对象的只读镜像，也能重建本次有界样本事实底座，还能从这套事实底座一键重建人工智能读取版和用户核对表格。
5. 只用批准的困难样本，生成一套测试事实底座，再由正式候选程序和正式配置生成两种测试版，并逐表、逐行、逐项核对两个版本是不是真正来自同一批事实。

当前结果身份仍然是：**第一工作包完成候选，待最新 Chat 和用户检查。**

第一次固定候选提交后，最新 Chat发现一个实质问题：当时Git中保存的正式候选程序只能重建输入镜像和事实底座，人工智能版和Excel版的实际制作方法仍留在临时运行区，未来窗口不能只靠正式程序和配置重新生成两个版本。用户随后授权把真实制作方法接回现有正式程序和配置，并重新生成、重新对账、重新提交检查。本版就是对这一项问题的定点修正，没有重新选择样本、改动事实底座或扩大业务范围。

## 二、本次用户纠正后的样本来源

用户明确要求：

> 不使用这份额外的汇总表，只用原来允许的八份文件，重新选择和排列测试样本。

因此，本次已废弃先前使用额外汇总表得出的样本计数、排序和选择。重新选样只使用下列八份原始事件文件：

1. `K05_ORDER_EVENT.csv`
2. `K05_FILL_EVENT.csv`
3. `K05_POSITION_ACTION_EVENT.csv`
4. `K05_CONDITIONAL_ORDER_LIFECYCLE_EVENT.csv`
5. `K05_FUND_EVENT.csv`
6. `K05_COPY_FUNDS_ONLY_EVENT.csv`
7. `K05_EVENT_LINKAGE_MATRIX.csv`
8. `K05_EVENT_LINEAGE.csv`

被排除的`K05_UNIFIED_EVENT_EVIDENCE.csv`没有进入本次输入镜像、候选池、排序计算、测试事实底座或两个测试版本。配置和样本清单中仅保留它的名称作为“明确禁止使用的对象”，不是作为数据来源。

## 三、重新排序后选中的困难样本

| 要验证的困难情形 | 候选数量 | 按固定规则选中的对象 | 必须保留的边界 |
|---|---:|---|---|
| 多次开仓或加仓尝试、撤单和替换 | 1 | T120 | 候选替换不写成已确定替换 |
| 止损止盈演变和候选关系 | 60 | T020 | 候选关系不升级为唯一归属 |
| T087同秒两条合法路径及外包络 | 合同固定 | T087 | 不挑选某一条写成真实唯一顺序 |
| 外部资金或共享账户影响 | 87 | T020 | 时间范围重叠只是共享影响线索，不证明资金事件由这笔交易造成或专属于它 |
| 相邻交易和无法可靠归属的条件对象 | 31 | T092、T093、COND-0218 | 条件对象仍然未归属，不强行塞进其中一笔，也不改写原来的候选周期字段 |
| 跟单影响对象 | 12 | U016 | 跟单对象与136笔主动交易分开 |
| 部分成交、减仓、平仓和仓位连续性 | 4 | T112 | 只验证已有事件的连续呈现，不补造事件 |
| 冲突、区间、多个候选或未知 | 31 | T087＋T092＋T093＋COND-0218 | 保留两条路径、外包络、多候选和未知，不强选一个答案 |
| 长对象、分块连续读取和两个同源表示 | 136 | T020 | 人工智能版和用户表格必须回到同一事实ID |

每个候选池的全部对象、计数来源、排序元组、名次、选中和未选原因，都保存在`09_有界验证样本清单候选.json`中，不依靠本页的摘要代替。

## 四、主要数量和对账结果

- 44项输入采用清单候选已形成：10项候选为直接使用，31项候选为限定抽取，3项候选为以后重新计算。这只是候选分类，正式采用数仍是零。
- 74次准确对象引用全部完成装载回执，对应72条不重复路径。身份核对没有发现缺失或指纹不一致。
- 对应关系共208条：32条组成部分、94条能力、44条输入候选、19条严格缺口处置、16条阶段三以后新发现、3条以后重新计算要求。
- 有界测试事实底座共3896条：1300条事实、1258条关系、1336条来源链、2条方法证据。
- 人工智能读取版和用户核对表格现在都由`06_构建与确定性装载程序候选.py`中的`build-views`入口，按照`07_构建配置_依赖与运行环境锁定候选.json`中的固定输入、映射、版式和运行环境，从上述3896条底座生成。以后窗口进入该目录运行配置中保存的一条命令，即可同时重建两个版本，不再需要临时脚本或临场设计。
- 人工智能读取版重新生成后与原候选逐字节一致；Excel重新生成后，七张工作表的名称与顺序、全部表头、全部显示值、各表行数、九类样本和74次输入回执均与事实底座及固定配置一致，没有公式错误。两次独立重建的Excel内部技术文件编号可以不同，因此整个`.xlsx`文件的SHA-256不作为内容相同的唯一判断；两次重建的完整工作表显示内容指纹均为`39a2bdd5887cfab5ae935e446a47b2768251407ef88e99a989162caf981c2910`。
- 十五项最小程序测试全部通过，包括稳定ID、长整数、小数精度、时间时区、币种单位、未知、区间、多候选、冲突、固定序列化、两个同源表示、选择器唯一解释和最小获准输入的可重复装载。
- 候选程序另外重建了全部74个获准输入对象；重建结果与本次镜像的74个文件名、字节内容和SHA-256全部相同。
- 同一固定输入和同一候选程序重复生成两次，事实底座逐字节相同，业务内容SHA-256均为`8c065227f19d0bb9eea2d096fd5f27dd2425069ce7e49562442f833871a5605f`。
- 用户核对表格已实际导出、重新打开、逐表检查和渲染查看，没有公式错误，七张工作表可读，并与事实底座对账一致。

## 五、三项重新计算现在处于什么状态

本次只对下列三项写清以后如果要算，需要什么输入、使用什么公式、产生什么输出和怎样检查：

1. 目标动作所在分钟的 Mark 价格区间；
2. T087同秒两条合法路径及外包络；
3. 35.xx期初精度与收益影响的联合区间。

**三项都没有运行真实全量重新计算。**本次程序测试只验证数据和状态能否正确保留，不是这三项的业务计算结果。

## 六、这次明确没有完成什么

- 没有正式采用44项候选，当前正式采用数是零。
- 没有运行三项真实全量重新计算。
- 没有确定最终事实底座和两个表示必须永久使用的文件形式。
- 没有建立136笔客观交易链。
- 没有进入第二或第三工作包。
- 没有修改04B，也没有把“候选闭合”写成“问题已经正式解决”。
- 没有修改任何原件、正式仓库主线或冻结成果。
- 没有开始正式交易分析。

## 七、当前必须继续保留的限制

- 第7项“历史扫描是否绝对完整”的限制仍然保留。
- 第9项“项目真实起点”的限制仍然保留。
- 04B第61、62、64、65、66项仍然留在各自正确的后续时点。
- 04B第67项只候选承接了本工作包直接相关的最小生产环境、白名单装载和隔离边界。
- 04B第68项继续暂停，失败的用户语言候选没有进入本次输入。
- 样本验证通过只能证明候选程序和候选表示在这些批准样本上可使用，不能扩大成136笔已经建成。

## 八、本次候选的准确停点

合同点名的15份候选已经形成。针对最新 Chat发现的生成方法缺口，正式程序和配置已经补入两个测试版的实际生成方法，AI版和Excel版已经由该正式入口重新生成；内部程序检查、事实对账、两次独立重建、用户表格逐项对账与七张工作表可视检查均已完成。这些修正版会与固定合同和04A一起，只提交到当前待检查分支，交给最新 Chat限定检查。

最新 Chat 检查之后，当前 Work 只能对照固定 Git 实物核对它的判断，然后向用户报告并停止。检查不通过不得自动修正；检查通过也不得自动验收、冻结或并入正式主线。
