# 第一工作包｜用户白话说明与执行回执（修正候选，待审查与用户核对）

## 一、这次到底修正了什么

第一工作包的目的，是在真正建立136笔客观交易链以前，把将来怎么取用材料、怎么连接事实、怎样保留未知、怎样生成AI读取版和用户核对版，先做成可重复检查的候选规格和小范围测试。

前一固定候选提交`5ace60d21ab562326bc286ffe99f5234136432a6`没有通过检查，主要原因不是“少写了一句说明”，而是几处关键关系仍然不够可靠：十九项缺口虽然数量齐全，但与阶段三原记录的实际含义存在错位风险；九十四项能力只有宽泛描述，不能直接告诉程序从哪里取什么、怎样连接、输出到哪里、怎样检查；AI版和用户版的组织方式仍不够适合连续读取和普通用户核对；质量回执又把部分“候选规格已经写出”说得像“能力已经跑完并通过”。

用户因此授权在有限范围内修正。第一轮修正形成固定候选`acfa17a22e2dcfe1780aa66c152d1c8ddc20d57b`后，新Work“交易分析work”再次检查出三处实质阻断：九十四项能力的“程序可执行”仍然被说大了；关系的来源事件还没有与来源事实严格对上；Excel有两个技术原文单元格超过了安全承载长度。用户随后授权“第二轮定点修正”。

两轮合起来，本候选实际处理的是：

1. 把十九项缺口逐项与阶段三机器总账第447至465行按真实含义对应，并加入“故意交换就必须失败”的检查。
2. 把九十四项能力分别写成程序可以读取的候选规格，共固定33个算子身份和10种声明性处理类型；逐字段说明来源、用途、原值、规范值、缺失处理、连接规则、输出位置和检查办法。本轮只运行锁定镜像的字段、连接和声明性处理见证，没有运行完整业务算子，也没有产生正式事实。
3. 补强候选程序、固定配置和九十四项程序测试，加入真实逻辑检查和“故意改坏就必须拒绝”的负向检查，而不是只检查文件里有没有某个词。程序现在会读完多结构JSONL的所有行再取字段并集，并拒绝使用中英文行号字段跨文件拼接。
4. 八份样本来源和T087方法对象现在都只从`02`回执点名的只读镜像取数；程序会拒绝错路径、错选择器、错字段指纹、错连接规则、错输出、错检查和被改坏的镜像。
5. 三项以后重新计算的候选合同已与`02`回执逐对象绑定，并检查精确公式、输出和检查项；本轮只用虚构数字验证办法，没有运行真实业务重算。
6. 关系验证除了要求关系两端的事实ID存在，现在还必须证明关系中登记的来源事件ID与来源事实内保存的事件ID完全一致；缺失或改错都会被拒绝。
7. 重新生成同一事实底座、AI读取版和用户核对版，并从对象连续性、事实ID、关系两端、来源事件、来源追溯和显示内容等方面重新对账。原先缺失目标端的六十三条对象关系，现在连到六条专用的“关系目标对象引用”事实；这六条不是交易事件，也没有把候选关系升级为确定关系。
8. Excel仍保持八张工作表，但最后一张“技术原文”对两条超长原文做无损分段。3,902条源记录对应3,905个分段行，每个单元格最多30,000个UTF-16代码单元；按分段序号直接连接后，3,902条全部与事实底座逐字一致。
9. 更新本回执、04B对应回执、自动质量检查和04A当前状态，使说明与实际文件一致。

本轮没有触发修改`04_数据结构与规范化规则候选.json`和`05_三项重新计算前置合同与依赖候选.json`的条件；`01`、`02`、`04`、`05`、`09`保持不变。前一失败提交继续保留为失败历史，没有被修改、覆盖或伪装成通过版本。

## 二、样本来源和禁止范围没有扩大

本轮仍然遵守用户已经确认的要求：

> 不使用这份额外的汇总表，只用原来允许的八份文件，重新选择和排列测试样本。

本次测试样本仍只来自下列八份原始事件文件：

1. `K05_ORDER_EVENT.csv`
2. `K05_FILL_EVENT.csv`
3. `K05_POSITION_ACTION_EVENT.csv`
4. `K05_CONDITIONAL_ORDER_LIFECYCLE_EVENT.csv`
5. `K05_FUND_EVENT.csv`
6. `K05_COPY_FUNDS_ONLY_EVENT.csv`
7. `K05_EVENT_LINKAGE_MATRIX.csv`
8. `K05_EVENT_LINEAGE.csv`

被排除的`K05_UNIFIED_EVENT_EVIDENCE.csv`没有进入输入镜像、候选池、排序、事实底座或两个测试版本。它只以“明确禁止使用的对象”出现在控制说明中。

## 三、困难样本仍是什么

| 要验证的困难情形 | 候选数量 | 选中对象 | 必须保留的边界 |
|---|---:|---|---|
| 多次开仓或加仓尝试、撤单和替换 | 1 | T120 | 候选替换不能写成已经确定的替换 |
| 止损止盈演变和候选关系 | 60 | T020 | 候选关系不能升级成唯一归属 |
| 同一秒内两条合法路径及其范围 | 合同固定 | T087 | 不能任选一条冒充真实唯一顺序 |
| 外部资金或共享账户影响 | 87 | T020 | 时间重叠不等于资金事件由该交易造成或只属于该交易 |
| 相邻交易和无法可靠归属的条件对象 | 31 | T092、T093、COND-0218 | 未归属对象不能强行塞入某笔交易 |
| 跟单影响对象 | 12 | U016 | 跟单对象与136笔主动交易分开 |
| 部分成交、减仓、平仓和仓位连续性 | 4 | T112 | 只呈现已有事件，不补造事件 |
| 冲突、区间、多个候选或未知 | 31 | T087、T092、T093、COND-0218 | 保留冲突、范围、多个候选和未知 |
| 长对象、连续读取和同源表示 | 136 | T020 | AI版和用户版必须回到同一事实ID |

完整候选池、排序依据、名次、选中和未选原因仍保存在未改动的`09_有界验证样本清单候选.json`中。

## 四、目前实际形成了什么

### 1. 输入和对应关系

- 四十四项输入采用清单候选仍是：10项直接使用候选、31项限定抽取候选、3项以后重新计算候选。它们都还没有被用户正式采用。
- 七十四次对象引用对应七十二条不重复物理路径，原有身份和装载回执保持不变。
- 对应关系仍是208条：32条组成部分、94条能力、44条输入候选、19条缺口、16条阶段三以后新发现、3条以后重新计算要求。
- 十九项缺口现在与阶段三第447至465行一一对应，分别保存阶段三能力编号、原缺口表述、原行号和原记录编号。程序会拒绝含义错位的交换映射。
- 九十四项能力现在都有具体候选规格。其中69项在锁定镜像上完成了字段规则、连接键和声明性处理类型的有界见证；另外25项只进入缺口、不适用或等待用户决定的状态门。这不是完整业务算子运行，不证明九十四项已在136笔全量数据上运行通过。

本轮实际检查4,773次字段应用，对应678个“输入对象＋字段”唯一组合。多结构JSONL按所有非空记录的字段并集检查，不再只看第一行。程序同时禁止用`source_row`、`manual_source_row`、“来源行号”等中英文行号字段当作跨文件连接键。

九十四项能力当前的真实状态是：

- 69项：已完成锁定镜像的字段、连接和声明性处理见证，但完整业务算子尚未运行；
- 19项：对应缺口，仍必须明确保留；
- 5项：只说明某类内容不适用或不得进入客观事实层；
- 1项：等待用户决定是否正式采用。

### 2. 三种同源结果

有界测试事实底座共有3,902条记录：1,306条事实、1,258条关系、1,336条来源链和2条方法证据。其中1,300条是样本事件，另外6条只是为了让关系目标能够反向查回的对象引用，不是新交易事件。

本轮重新生成后的实际身份为：

- 事实底座：19,279,441字节；SHA-256为`2bc3b9ef2574eddee3e496e688456cbabf7fc569955cebb0a067545ddb2484f9`。
- AI读取版：19,290,140字节；SHA-256为`c01a121bac57cf250d9a691f44e4555c423bb5e9fbb1f13158afa3d076cea70c`。
- 用户核对版：1,444,675字节；当前文件SHA-256为`a8947e8966a79a78f7add1a4eddc46a1f9673efd71a3e91b06798a3c3f20b883`。

AI读取版不再按固定条数机械分块，而是先给出对象目录，再按主动交易、共享账户、无法归属对象、跟单对象、跨对象关系、T087方法和来源追溯连续排列。每条完整机器记录只在所属对象正文中出现一次，目录只保存指向正文的引用。

用户核对版现在有八张中文工作表：

1. 从这里开始
2. 对象目录
3. 事件时间线
4. 关系与归属
5. 特殊对象与边界
6. 来源追溯
7. 输入范围
8. 技术原文

前七张用于普通阅读和核对，第八张保存无损分段的技术原文。其中3,900条记录各占1行，两条超长记录分别占3行和2行，共增加3个分段行。实际生成后的Excel内部文件扫描和表格工具重新打开对账都已通过，没有单元格超过30,000个UTF-16代码单元，也没有原文截断。但是，本轮没有实际使用Microsoft Excel桌面程序完成“打开、保存、再读取”测试，也不能由程序代替用户宣布“已经看懂”。

### 3. 重复生成和程序测试

- 使用同一最终程序、配置和输入，在两个新的独立临时位置重新生成，事实底座和AI读取版均逐字节一致。
- 两次Excel文件内部技术编号可能不同，所以不以整个压缩文件的字节完全相同作为唯一标准；本轮八张工作表的名称、顺序、表头和全部应有显示值对账通过，显示内容指纹为`3c4b115b7c9e94777fc4efbe02897aaf7d0082bbe21b3b709c0f0479d3c7ab55`。
- 九十四项程序测试实际运行结果为94项通过、0项失败。它们除了覆盖原有长编号、小数、负数、时间时区、币种单位、未知、区间、多个候选、不适用、冲突、十九项对应和T087范围，还会故意改坏操作类型、字段规则、多结构JSONL字段并集、中英文行号连接键、关系来源事件、Excel分段数量和文字，确认这些错误都会被拒绝。
- 三项公式测试只使用虚构数据验证程序，不是三项真实业务重新计算。

这些结果证明当前候选在批准样本和当前技术检查范围内可以被重复生成并完成内部对账；它们不证明另一个AI已经真实读完并正确理解，也不证明用户本人已经看懂，更不证明136笔客观交易链已经完成。

## 五、三项重新计算的实际状态

本次仍只把三项以后重新计算的目标、输入、公式、输出和检查办法写成候选：

1. 目标动作所在分钟的Mark价格区间；
2. T087同一秒两条合法路径及其外包范围；
3. 35.xx期初精度与收益影响的联合区间。

三项真实业务重新计算均未运行，也未获得运行授权。

## 六、这次明确没有完成什么

- 没有正式采用四十四项候选；正式采用数仍为零。
- 没有把十九项缺口写成全部解决。
- 没有把九十四项候选规格写成全量能力已经运行通过。
- 没有运行三项真实业务重新计算。
- 没有确定最终事实底座、AI版和用户版必须永久使用的文件形式。
- 没有建立136笔客观交易链。
- 没有进入第二或第三工作包。
- 没有修改正式04B或任何冻结成果。
- 没有开始正式交易分析。

## 七、当前停点

第二轮定点修正已经在原待检查分支的本地隔离工作区形成。用户批准可以受影响的范围共十一份，但实际产生差异的只有：`03`、`06`、`07`、`08`、`12`、`13`、本回执`00`、`14`和`04A`，共九份文件。`10`事实底座和`11`AI读取版经重建检查后保持原有字节身份，不进入本次差异。`04`和`05`的条件修改没有触发；`01`、`02`、`09`及其他正式和冻结材料保持不变。

下一步只允许把上述九份实际变更形成一个新的固定候选提交，上传到同一待检查分支，再交给新Work“交易分析work”作只读审查。新Work需要检查固定提交中的实际文件、能力声明边界、关系来源事件一致性、AI版实际读取能力和用户版结构，但不能代替用户确认“用户已经看懂”。

新Work审查不通过时不得自动修正；审查通过也不等于用户正式验收。当前没有正式采用、真实重新计算、136笔构建、04B修改、正式主线合并、第二工作包或正式交易分析权限。
